# v0.6.1 — Bug Fixes & Improvements

## Bug Fixes
- generateImageAndSave / generateVideoAndSave loads config directly from file
- loadImageDefaults / loadVideoDefaults falls back to reading from file
- SSE keepalive comment detection fixed
- JSON parse error now shows first 200 chars of response body

## Improvements
- -v now shows tool call parameters with per-key truncation
- midjourney_imagine description notes higher cost, prefer generate_image
- apimart-cli defaults to chat mode when run with no arguments
- AGENTS.md adds semantic versioning rules
