# APIMart CLI Configuration
# Place this file at ~/.config/openai/config.yaml or ~/.config/apimart/config.yaml
# or pass --config /path/to/config.yaml at runtime

# API key (can also be set via OPENAI_API_KEY or APIMART_API_KEY env var)
api_key: "your-api-key-here"

# API base URL (can also be set via OPENAI_BASE_URL or APIMART_API_BASE env var)
# base_url: "https://api.apimart.ai"

# Generation mode: auto (detect from base_url), sync, async, auto.
# Default: auto
# mode: "auto"

# HTTP proxy (can also be set via OPENAI_HTTP_PROXY or APIMART_HTTP_PROXY env var, or --http-proxy CLI flag)
# http_proxy: "http://127.0.0.1:7890"

# Verbose output: show full result JSON (can also use --verbose / -v CLI flag)
# verbose: true

# Save prompt text to image_{task_id}.md file (can also use --save-prompt CLI flag)
# save_prompt: true

# OpenRouter app attribution (optional)
# Set via env vars: OPENAI_REFERER="https://your-site.com" OPENAI_APP_TITLE="Your App"

# Default values for generation parameters (per modality)
defaults:
  image:
    model: "gpt-image-2-official"
    # Most economical: 1536x512·Low → $0.00144/image
    size: "3:1" # 3:1 + 1k → 1881x836 / 1536x512
    resolution: "1k" # Lowest resolution tier (APIMart only)
    quality: "low" # Fastest/cheapest quality tier
    output_format: "png"
    # style: "vivid"       # OpenAI: vivid or natural
    # n: 1
    # output_compression: 85

  video:
    model: "doubao-seedance-2.0" # APIMart only
    # size: "16:9"
    # resolution: "480p"
    # duration: 5

  chat:
    model: "gpt-4o" # default chat model (change to your preferred model)
